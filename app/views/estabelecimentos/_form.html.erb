<%= form_with(model: estabelecimento, :html => { :class => "form-horizontal" }, local: true) do |form| %>
  <% if @estabelecimento.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <div class="alert-heading">
        <h3 class="alert-title"><%= pluralize(@estabelecimento.errors.count, t('error')) %> <%= pluralize_without_count(@estabelecimento.errors.count, t('found')) %>:</h3>
      </div>
      <div class="alert-body">
        <ul>
        <% @estabelecimento.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :nome, t('nome') %>
    <%= form.text_field :nome, id: :estabelecimento_nome, :class => "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :endereco, t('endereco') %>
    <%= form.text_field :endereco, id: :estabelecimento_endereco, :class => "form-control" %>
  </div>

  <div class="form-check">
    <%= form.check_box :ativo, id: :estabelecimento_ativo, :class => "form-check-input" %>
    <%= form.label :ativo, t('ativo'), :class => "form-check-label" %>
  </div>

  <div class="form-group mt-2">
    <%= form.submit t('.save', :default => t('helpers.links.save')), :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t('helpers.links.cancel')),
      estabelecimentos_path, :class => 'btn btn-secondary' %>
  </div>
<% end %>
